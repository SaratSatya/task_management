# Task Management App

A full-stack task management application built with Next.js, MongoDB, and Vercel.

## Stack
- Next.js App Router
- MongoDB with Mongoose
- JWT authentication with HTTP-only cookies
- bcryptjs
- Zod

## Environment Variables

Create `.env.local` with:

```env
MONGODB_URI=
JWT_SECRET=
TASK_ENCRYPTION_KEY=